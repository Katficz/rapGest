extends layout

block content
   div(class="col mx-md-5 my-5")
      h1 Operacja

      p.h4.pt-3  Nazwa: #{operation.name==null ? '' : operation.name}
      p.h4.pt-3  Opis: #{operation.description==null ? '' : operation.description}

     
      a.btn.btn-danger.my-5.mr-2(href=operation.url + '/delete') Usuń 
      a.btn.btn-primary.my-5.mr-2(href=operation.url + '/update') Zmodyfikuj 

  

   script.
      const activateLink = document.querySelector('#operationListActive')
      activateLink.className+=" active"
      const activateSubMenu = document.querySelector('#deviceSubMenu')
      activateSubMenu.className+=" active"
